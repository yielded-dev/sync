# @yielded/sync-local-indexeddb

IndexedDB local persistence for Effect Sync clients.

This package is a scaffold. Its entry points intentionally export no API yet.
See the [repository README](../../README.md) for package boundaries and development commands.
